name: Swift on ARM64 Linux

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    arch: arm64
    container:
      image: swift
    steps:
    - uses: actions/checkout@v2
    - name: Build and test
      run: |
        swift -version
        uname -a
