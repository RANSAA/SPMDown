name: Swift on ARM64 Linux

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: swift
      arch: arm64
    steps:
    - uses: actions/checkout@v3
    - name: Build and test
      run: |
        uname -a
        swift -version
