name: Swift on ARM64 Linux

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: swift
      platform: arm64
    steps:
    - uses: actions/checkout@v2
    - name: Build and test
      run: |
        swift -version
        uname -a
